{
  "name": "truthcast",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/web",
    "build": "npm run build --workspace=packages/web",
    "test-solana": "node packages/pipeline/scripts/test-solana.js",
    "test-audio": "node serve-test.js"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
